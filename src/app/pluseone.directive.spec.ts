import { PluseoneDirective } from './pluseone.directive';

describe('PluseoneDirective', () => {
  it('should create an instance', () => {
    const directive = new PluseoneDirective();
    expect(directive).toBeTruthy();
  });
});
